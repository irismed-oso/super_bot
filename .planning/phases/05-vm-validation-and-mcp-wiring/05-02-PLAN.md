---
phase: 05-vm-validation-and-mcp-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - DEPLOY.md
  - systemd/superbot.service
autonomous: false
requirements:
  - MCPW-03
  - VMEV-01
  - VMEV-02
  - VMEV-03
user_setup:
  - service: mic_transformer_config
    why: "Credential YAML files must be copied from local dev machine to VM"
    dashboard_config:
      - task: "SCP config/*.yml files to VM"
        location: "Local machine: scp ~/mic_transformer/config/*.yml bot@VM_IP:/home/bot/mic_transformer/config/"

must_haves:
  truths:
    - "mcp[cli]~=1.26.0 is installed in mic_transformer .venv on the VM"
    - "mic_transformer config/*.yml credential files exist on the VM"
    - "systemd .env file uses bare KEY=VALUE syntax (no export, no interpolation)"
    - "MCP server cold-start completes within 60 seconds on VM"
    - "Sending 'check pipeline status' via Slack triggers the MCP deploy_version or check_pipeline_status tool and returns real data"
  artifacts:
    - path: "DEPLOY.md"
      provides: "v1.2 Phase 5 deployment section"
      contains: "v1.2"
  key_links:
    - from: "bot/agent.py (_build_mcp_servers)"
      to: "/home/bot/mic_transformer/.claude/mcp/mic-transformer/server.py"
      via: "stdio subprocess spawn"
      pattern: "mic-transformer"
    - from: "server.py"
      to: "/home/bot/mic_transformer/config/*.yml"
      via: "os.chdir(PROJECT_ROOT) + relative file load"
      pattern: "config"
---

<objective>
Deploy Phase 5 to VM, validate all prerequisites, and prove end-to-end MCP connectivity with a real tool call through Slack.

Purpose: This is the deployment and validation phase. All code changes were made in Plan 01. This plan runs the deploy script on the VM, resolves any issues found during validation (env audit, cold-start benchmark, network connectivity), and confirms end-to-end MCP tool calls work through Slack.

Output: Working mic-transformer MCP server on VM, updated DEPLOY.md with v1.2 section, verification checkpoint confirming real tool call through Slack.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vm-validation-and-mcp-wiring/05-CONTEXT.md
@.planning/phases/05-vm-validation-and-mcp-wiring/05-RESEARCH.md
@.planning/phases/05-vm-validation-and-mcp-wiring/05-01-SUMMARY.md
@scripts/deploy_v1.2_phase5.sh
@DEPLOY.md
@systemd/superbot.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.2 Phase 5 section to DEPLOY.md</name>
  <files>DEPLOY.md</files>
  <action>
Add a "v1.2: MCP Parity - Phase 5" section to DEPLOY.md covering:

1. **Prerequisites checklist:**
   - config/*.yml files copied to VM via SCP (list all 7 files: config.yml, secrets.yml, gcs_utils_config.yml, db_irismedapp.yml, db_crystalpm_mirror.yml, clinic_gdrive_config.yml, clinic_gdrive_eyemed_config.yml)
   - Network access from VM to production server (136.111.85.127:8080) verified
   - mcp[cli]~=1.26.0 installed in mic_transformer .venv

2. **Deployment steps:**
   - Reference deploy script: `bash scripts/deploy_v1.2_phase5.sh`
   - Note that config files must be SCP'd separately before running the script

3. **Troubleshooting:**
   - MIC_TRANSFORMER_MCP_DISABLED=1 to disable mic-transformer MCP for troubleshooting
   - Cold-start timeout: pre-compile .pyc by running import test manually
   - Missing config files: check server.py stderr in journalctl

4. **Verification:**
   - Slack test: @SuperBot check pipeline status for Beverly today
   - Expected: real pipeline status data returned in thread

Follow the existing DEPLOY.md formatting conventions.
  </action>
  <verify>
    <automated>grep -c "v1.2" /Users/hanjing/Documents/Code/GitLab/claude_workspace/super_bot/DEPLOY.md</automated>
    <manual>DEPLOY.md has v1.2 section with prerequisites, steps, troubleshooting, verification</manual>
  </verify>
  <done>DEPLOY.md contains v1.2 Phase 5 section with all deployment prerequisites, steps referencing the deploy script, troubleshooting guidance, and Slack verification instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to VM and verify end-to-end MCP connectivity</name>
  <what-built>
Phase 5 code changes (feature flag in config.py, updated _build_mcp_servers() in agent.py) and deploy script (scripts/deploy_v1.2_phase5.sh) were created in Plan 01. DEPLOY.md updated with v1.2 section in Task 1 above.
  </what-built>
  <how-to-verify>
1. **Copy config files to VM** (must be done before deploy script):
   ```
   scp ~/path/to/mic_transformer/config/*.yml bot@VM_EXTERNAL_IP:/home/bot/mic_transformer/config/
   ```

2. **Run the deploy script:**
   ```
   bash scripts/deploy_v1.2_phase5.sh
   ```

3. **Check each step's output:**
   - Step 1: "MCP SDK OK" printed (mcp[cli] installed successfully)
   - Step 3: ".env syntax OK" or fix any flagged lines
   - Step 4: Cold-start benchmark under 30s = good, 30-60s = warn, >60s = must fix
   - Step 5: HTTP 200 from production API (network access works)
   - Step 6: superbot service active
   - Step 7: Logs show mcp_server_count >= 1 with mic-transformer listed

4. **Test via Slack:**
   Send in the SuperBot channel:
   ```
   @SuperBot check pipeline status for Beverly today
   ```
   Expected: Bot responds with real pipeline status data (not an error about missing tools or credentials).

5. **Test disable flag (optional):**
   - SSH to VM, add `MIC_TRANSFORMER_MCP_DISABLED=1` to /home/bot/.env
   - Restart: `sudo systemctl restart superbot`
   - Check logs show "mcp.mic_transformer_disabled_by_env"
   - Remove the line and restart to re-enable

**If cold-start exceeds 60 seconds:**
   - The deploy script will report this in Step 4
   - Pre-compile .pyc: run the import test command once manually on VM
   - If still too slow after .pyc compilation, add ExecStartPre to systemd/superbot.service (see RESEARCH.md for the command)
  </how-to-verify>
  <resume-signal>Type "approved" with the Slack response screenshot or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- DEPLOY.md contains v1.2 Phase 5 section
- Deploy script ran successfully on VM (all steps passed)
- Cold-start benchmark under 60 seconds
- systemd .env has no export/interpolation syntax issues
- Slack @mention with "check pipeline status" returns real data from the MCP tool
- MIC_TRANSFORMER_MCP_DISABLED flag works when set (optional verification)
</verification>

<success_criteria>
- mcp[cli]~=1.26.0 installed and verified in mic_transformer .venv on VM
- config/*.yml credential files present on VM
- systemd .env validated for syntax compatibility
- MCP server cold-start under 60 seconds on VM hardware
- One confirmed end-to-end tool call through Slack proves full MCP connectivity
</success_criteria>

<output>
After completion, create `.planning/phases/05-vm-validation-and-mcp-wiring/05-02-SUMMARY.md`
</output>
