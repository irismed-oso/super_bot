---
phase: 07-mutation-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MTTL-03, MTTL-04, MTTL-06, MTTL-07]

must_haves:
  truths:
    - "User can trigger autopost with dry_run from Slack and receive the dry-run report"
    - "User can trigger posting prep from Slack and receive confirmation"
    - "User can trigger Azure mirror sync from Slack and receive confirmation"
    - "User can trigger benefits fetch from Slack or document why it was skipped"
  artifacts: []
  key_links:
    - from: "SuperBot agent session"
      to: "mic-transformer MCP server"
      via: "stdio subprocess tool call"
      pattern: "vsp_autopost|posting_prep|azure_mirror_trigger|vision_benefits_fetch"
    - from: "autopost tools"
      to: "run_revolution_poster.py subprocess"
      via: "subprocess call with dry_run=True"
      pattern: "dry_run.*True"
    - from: "benefits fetch"
      to: "Prefect API at 136.111.85.127:4200"
      via: "HTTP POST to create deployment run"
      pattern: "shen:tofu.*4200"
---

<objective>
Validate subprocess-based and Prefect-triggered mutation MCP tools on the VM: autopost (dry_run), posting prep, Azure mirror sync, and benefits fetch.

Purpose: Confirm that higher-risk mutation tools -- those running subprocesses or triggering Prefect deployments -- can be invoked through SuperBot safely. Autopost uses dry_run=True by default, providing built-in safety.
Output: Validation results for 4 tool categories confirming subprocess execution and Prefect connectivity.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-read-only-status-and-storage-tools/06-01-SUMMARY.md
@.planning/phases/06-read-only-status-and-storage-tools/06-02-SUMMARY.md
@.planning/phases/07-mutation-tools/07-CONTEXT.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate autopost dry_run and posting prep tools</name>
  <files></files>
  <action>
SSH to VM and run direct agent calls to validate 2 tool categories.

```
sudo -u bot bash -c 'cd /home/bot/super_bot && .venv/bin/python /home/bot/test_mcp.py "PROMPT"'
```

**Tool 1: vsp_autopost with dry_run=True** (MTTL-03)
- Prompt: Ask the agent to run VSP autopost for Beverly in dry_run mode for today (or a recent date with known data)
- The tool defaults to dry_run=True -- safe to execute
- This runs run_revolution_poster.py as subprocess with dry_run flag
- Expected: Dry-run report showing what would be posted, or "no AIOUT files to post"
- Requires: GCS credentials (confirmed in Phase 6) + Revolution EMR credentials in mic_transformer config
- If Revolution EMR credentials are missing, document the specific error
- Use max_turns=5

**Tool 2: posting_prep** (MTTL-04)
- Prompt: Ask the agent to run posting prep for Beverly for a recent date
- This tool uploads remits to GDrive and generates task sheets
- Requires: GCS + GDrive credentials (both confirmed in Phase 6)
- Expected: Confirmation of upload or "nothing to prep"
- Use max_turns=5

Do NOT test eyemed_autopost separately -- vsp_autopost validates the same subprocess pathway. If vsp_autopost works, eyemed_autopost uses the identical mechanism.

Record each tool's invocation status, output, and any credential or subprocess errors.
  </action>
  <verify>
    Both tools invoke successfully:
    - vsp_autopost: Tool called with dry_run=True, subprocess executed, report returned (or clear error about missing Revolution EMR credentials)
    - posting_prep: Tool called, GCS/GDrive accessed, prep result returned
    NOT acceptable: Tool not found, MCP server errors, silent failures
  </verify>
  <done>Autopost dry_run and posting_prep confirmed callable. Dry_run safety mechanism verified working.</done>
</task>

<task type="auto">
  <name>Task 2: Validate Azure mirror sync and benefits fetch tools</name>
  <files></files>
  <action>
SSH to VM and run direct agent calls to validate 2 more tool categories.

**Tool 3: azure_mirror_trigger** (MTTL-06)
- Prompt: Ask the agent to trigger Azure mirror sync (Hop 1 sync via Prefect)
- This tool uses SSH + Prefect API to trigger a sync deployment
- Known issue: SSH publickey denied for ansible user (from Phase 6) may block this
- Expected outcomes:
  a) Tool invokes and triggers sync successfully
  b) SSH publickey denied -- document this as known limitation consistent with Phase 6 findings
- Use max_turns=5
- If SSH fails, the tool invocation itself is still validated (correct MCP tool found and called)

**Tool 4: vision_benefits_fetch** (MTTL-07)
- RISK: This tool polls Prefect for up to 10 minutes. SuperBot session timeout is also ~10 minutes.
- Claude's discretion on whether to test or skip:
  - Option A (test): Use max_turns=3 to limit exposure. The tool triggers a Prefect deployment via HTTP POST to 136.111.85.127:4200 with auth shen:tofu. Even if polling times out, the initial trigger confirms connectivity.
  - Option B (skip with documentation): If session timeout risk is too high, document why and confirm the tool exists in the MCP server
- Prompt (if testing): Ask the agent to fetch VSP vision benefits for a test patient, noting it may take up to 10 minutes
- Expected: Either Prefect deployment triggered (confirms API connectivity) or documented skip with rationale

Record results for both tools. For azure_mirror_trigger, note whether the SSH limitation from Phase 6 persists.
  </action>
  <verify>
    Tool validation results:
    - azure_mirror_trigger: Tool invoked (success or SSH limitation documented)
    - vision_benefits_fetch: Tool invoked or skip documented with rationale
    NOT acceptable: Tool not found in MCP server, MCP server crash
  </verify>
  <done>Azure mirror sync and benefits fetch validated (or limitations documented). All Phase 7 mutation tool categories confirmed callable through SuperBot.</done>
</task>

</tasks>

<verification>
All 4 higher-risk mutation tool categories (autopost, posting prep, Azure mirror sync, benefits fetch) are validated through SuperBot on the VM. Any tools that cannot complete due to infrastructure limitations (SSH, session timeout) are documented with specific root causes.
</verification>

<success_criteria>
- Autopost dry_run report returned (or Revolution EMR credential gap documented)
- Posting prep invokes successfully
- Azure mirror trigger invokes (SSH limitation from Phase 6 may recur -- acceptable if documented)
- Benefits fetch either invokes or is documented as skipped with clear rationale
- No code changes required (validation only)
</success_criteria>

<output>
After completion, create `.planning/phases/07-mutation-tools/07-02-SUMMARY.md`
</output>
