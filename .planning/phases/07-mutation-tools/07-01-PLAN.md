---
phase: 07-mutation-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MTTL-01, MTTL-02, MTTL-05, MTTL-08]

must_haves:
  truths:
    - "User can trigger VSP Gemini extraction from Slack and receive dispatch confirmation"
    - "User can trigger EyeMed Gemini extraction from Slack and receive dispatch confirmation"
    - "User can trigger AIOUT reduction from Slack and receive result or job status"
    - "User can ingest a manual PDF into the pipeline from Slack"
    - "User can requeue missing extraction pages from Slack"
  artifacts: []
  key_links:
    - from: "SuperBot agent session"
      to: "mic-transformer MCP server"
      via: "stdio subprocess tool call"
      pattern: "vsp_extract|eyemed_extract|reduce_aiout|ingest_pdf|requeue_missing_pages"
    - from: "MCP tools"
      to: "Production API at 136.111.85.127:8080"
      via: "HTTP POST requests"
      pattern: "HTTP.*136\\.111\\.85\\.127:8080"
---

<objective>
Validate API-triggered mutation MCP tools on the VM: extraction (VSP + EyeMed), reduction, PDF ingestion, and page requeue.

Purpose: Confirm that all HTTP-API-based mutation tools can be invoked through SuperBot and reach the production API successfully. These tools have the lowest blast radius since the production API validates all inputs.
Output: Validation results for 5 tools confirming API connectivity and correct invocation.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-read-only-status-and-storage-tools/06-01-SUMMARY.md
@.planning/phases/06-read-only-status-and-storage-tools/06-02-SUMMARY.md
@.planning/phases/07-mutation-tools/07-CONTEXT.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate extraction and requeue tools via direct agent calls</name>
  <files></files>
  <action>
SSH to VM and run direct agent calls to validate 3 tools. Use the established pattern from Phase 6:

```
sudo -u bot bash -c 'cd /home/bot/super_bot && .venv/bin/python /home/bot/test_mcp.py "PROMPT"'
```

**Tool 1: vsp_extract** (MTTL-01)
- Prompt: Ask the agent to trigger VSP Gemini extraction for Beverly for today's date
- The tool makes an HTTP POST to production API (136.111.85.127:8080) which dispatches to Celery workers
- Expected: Tool invoked, API responds with dispatch confirmation or "no files to process"
- Either outcome confirms the tool is callable and the API connection works

**Tool 2: eyemed_extract** (MTTL-01)
- Prompt: Ask the agent to trigger EyeMed Gemini extraction for Beverly for today's date
- Same pattern as vsp_extract, different endpoint
- Expected: Dispatch confirmation or "no files to process"

**Tool 3: requeue_missing_pages** (MTTL-08)
- Prompt: Ask the agent to check for and requeue any missing extraction pages for Beverly
- Expected: Tool invoked, API responds (may report "no missing pages" which is fine)

Use max_turns=5 for each call. Beverly is the confirmed reliable test location from Phase 6.

Record each tool's: invocation status, API response, and any errors. If a tool fails due to credentials or network issues, document the specific error for troubleshooting.
  </action>
  <verify>
    All 3 tools invoke successfully (tool was called, API responded). Acceptable outcomes:
    - Success with real data processed
    - Success with "nothing to process" (no files for today)
    - API error with clear error message (proves connectivity works, just no actionable data)
    NOT acceptable: MCP tool not found, connection refused, credential errors
  </verify>
  <done>vsp_extract, eyemed_extract, and requeue_missing_pages all confirmed callable through SuperBot with production API responding</done>
</task>

<task type="auto">
  <name>Task 2: Validate reduction and PDF ingestion tools via direct agent calls</name>
  <files></files>
  <action>
SSH to VM and run direct agent calls to validate 2 more tools using the same pattern.

**Tool 4: reduce_aiout** (MTTL-02)
- Prompt: Ask the agent to reduce AIOUT for Beverly for today's date (or a recent date with known data)
- The tool does a GCS pre-check for json_files completeness, then makes HTTP POST to production API
- Expected: Tool invoked, either reduces successfully or reports "extraction not complete" (both valid)
- Note: This tool may take longer (5-10 min timeout on the API side). Use max_turns=5.

**Tool 5: ingest_pdf** (MTTL-05)
- Prompt: Ask the agent about the ingest_pdf tool -- what parameters it expects and whether it can be invoked
- Since we don't have a manual PDF readily available, validate that the tool is callable by asking the agent to describe its interface or attempt a call with a test path
- If a real test PDF exists in a known location (check GCS or S3), use that. Otherwise, confirm the tool exists, is callable, and the API endpoint is reachable
- Expected: Tool responds with parameter requirements or attempts invocation

Use max_turns=5 for each call. Document results.

If reduce_aiout fails due to no extractable data, that's acceptable -- the API connection and tool invocation are what matter.
  </action>
  <verify>
    Both tools invoke successfully:
    - reduce_aiout: Tool called, GCS pre-check runs, API contacted (success or "not ready" both acceptable)
    - ingest_pdf: Tool callable, parameters confirmed, API reachable
    NOT acceptable: Tool not found, connection refused, MCP server errors
  </verify>
  <done>reduce_aiout and ingest_pdf confirmed callable through SuperBot. All 5 API-triggered mutation tools validated.</done>
</task>

</tasks>

<verification>
All 5 API-triggered mutation tools (vsp_extract, eyemed_extract, reduce_aiout, ingest_pdf, requeue_missing_pages) are confirmed callable through SuperBot on the VM. Each tool successfully reaches the production API at 136.111.85.127:8080.
</verification>

<success_criteria>
- At least 4 of 5 tools invoke successfully (tool called, API responds)
- Any failures are documented with specific error messages and root cause
- No code changes required (validation only)
</success_criteria>

<output>
After completion, create `.planning/phases/07-mutation-tools/07-01-SUMMARY.md`
</output>
