---
phase: 03-end-to-end-integration
plan: "05"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-04"
files_modified: []
autonomous: false
requirements:
  - AGNT-03
  - AGNT-04
  - AGNT-05
  - GITC-01
  - GITC-02
  - GITC-03
  - GITC-04
  - GITC-05

must_haves:
  truths:
    - "A task @mention in Slack triggers real Claude Code execution and the thread receives a started update within seconds"
    - "A Q&A @mention returns Claude's answer in the thread with no worktree created"
    - "A code-change @mention produces a GitLab MR link in the thread pointing to a real branch"
    - "After a code change, pytest results appear in the thread"
    - "Two simultaneous @mentions execute sequentially without error"
    - "An error or timeout posts a meaningful error message to the thread, not silence"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete end-to-end integration live on the deployed bot. All Phase 3 success criteria must be confirmed by Nicole running real Slack interactions.

Purpose: No automated test can substitute for confirming the full Slack → Claude Code → GitLab → Slack loop works in production.
Output: Verified Phase 3 complete.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy Phase 3 code to VM and restart bot service</name>
  <files></files>
  <action>
Deploy the Phase 3 code changes to the GCP VM and restart the systemd service. Run these steps:

1. SSH to the VM as the deployment user
2. Pull the latest code in `/home/bot/super_bot` (or wherever the bot repo lives on the VM):
   ```bash
   sudo -u bot bash -c "cd /home/bot/super_bot && git pull origin main"
   ```
3. Install any new dependencies (none expected for Phase 3, but run for safety):
   ```bash
   sudo -u bot bash -c "cd /home/bot/super_bot && uv pip install -r requirements.txt"
   ```
4. Restart the systemd service:
   ```bash
   sudo systemctl restart superbot
   ```
5. Verify the service is running and healthy:
   ```bash
   sudo systemctl status superbot
   sudo journalctl -u superbot -n 30 --no-pager
   ```
6. Confirm no import errors in journal output (look for `started` log line from structlog)

If the service fails to start, read the journal output and diagnose. Common causes: missing import (`bot/worktree.py` or `bot/progress.py` not committed), syntax error in handlers.py, `on_message` field not added to `QueuedTask`.
  </action>
  <verify>
    <automated>MISSING — verification is via SSH to VM; run: ssh bot@VM "sudo systemctl is-active superbot" to confirm active status</automated>
    <manual>SSH to VM, run `sudo systemctl status superbot` — service should show `active (running)`. Check `sudo journalctl -u superbot -n 30` for no Python tracebacks.</manual>
  </verify>
  <done>Bot service is `active (running)` on VM with no import errors in journal. Latest Phase 3 code is deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Slack integration verification</name>
  <action>Nicole verifies the full Slack → Claude Code → GitLab → Slack loop using 4 test scenarios in the live bot channel.</action>
  <verify>
    <automated>MISSING — verification is live Slack interaction in production channel</automated>
    <manual>
Test the following scenarios in the Slack channel. Each should take under 5 minutes.

**Test 1 — Q&A task (no worktree):**
```
@SuperBot what is the purpose of the deduplication module in this bot?
```
Expected:
- Thread reply within ~5 seconds: "Working on it: what is the purpose..."
- Milestone: "Reading files..." appears in thread
- Final answer: Claude explains the deduplication module
- No MR link in response
- On VM, confirm: `ls /home/bot/worktree-*` does NOT create a new directory

**Test 2 — Code-change task (worktree + MR):**
```
@SuperBot add a docstring to the is_allowed function in bot/access_control.py and create an MR
```
Expected:
- Thread: "Working on it: add a docstring..."
- Milestones in order: "Reading files..." → "Making changes..." → "Committing changes..." → "Creating MR..."
- Final reply includes a `https://gitlab.com/.../merge_requests/NNN` link
- On VM: `ls /home/bot/worktree-*` shows a directory matching the thread_ts
- On GitLab: MR exists on a `superbot/...` branch targeting `develop`; MR description includes the Slack thread link

**Test 3 — Concurrent tasks (queue serialization):**
Send two @mentions within 2 seconds of each other:
```
@SuperBot what does session_map.py do?
@SuperBot what does formatter.py do?
```
Expected:
- Both threads eventually receive answers
- Second task may show a brief "queued" message
- No crashes or missing responses

**Test 4 — Error recovery (optional):**
Ask Claude something that will likely fail:
```
@SuperBot read the file /nonexistent/path.py and summarize it
```
Expected:
- Thread receives a meaningful error message, not silence
- Bot remains operational after the error (ask another Q&A question to verify)
    </manual>
  </verify>
  <done>All 4 test scenarios produce expected Slack thread behavior. Bot service remains stable. GitLab shows real MR from Test 2 on superbot/... branch targeting develop.</done>
  <what-built>
Full Phase 3 end-to-end integration:
- _run_agent_real replaces stub in handlers.py
- bot/worktree.py: git worktree lifecycle for code-change tasks
- bot/progress.py: milestone detection and thread posting
- bot/agent.py: cwd and on_message parameters
- bot/queue_manager.py: cwd and on_message fields on QueuedTask
- bot/formatter.py: format_mr_link, format_test_result added
- glab installed and authenticated on VM (Plan 01)
  </what-built>
  <resume-signal>Type "phase 3 verified" after all passing tests, or describe which tests failed and the error output from the Slack thread</resume-signal>
</task>

</tasks>

<verification>
All 4 test scenarios produce the expected Slack thread behavior. Bot service remains stable after test run. GitLab shows a real MR from Test 2 on a `superbot/...` branch.
</verification>

<success_criteria>
Phase 3 success criteria from ROADMAP.md all met:
1. Thread receives "started" update within seconds, completion when Claude finishes
2. Failures post error messages with context
3. Code-change request produces a real GitLab MR link in the thread
4. pytest results posted after code changes (via Claude's judgment)
5. Each task runs in isolated worktree — confirmed by `ls /home/bot/worktree-*` showing per-thread directories
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-integration/03-05-SUMMARY.md`
</output>
