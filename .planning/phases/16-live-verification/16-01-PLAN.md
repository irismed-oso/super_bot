---
phase: 16-live-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VRFY-01, VRFY-02, VRFY-03, VRFY-04]

must_haves:
  truths:
    - "Daily digest fires on VM and includes changelog section (or clean 'No changes' when idle)"
    - "Fast-path crawl and status commands respond in-place within seconds on production Slack"
    - "Batch crawl triggers all locations, posts progress updates without blocking agent queue"
    - "Heartbeat edits progress message during long sessions, completion shows elapsed time"
  artifacts: []
  key_links:
    - from: "Slack channel"
      to: "superbot-vm systemd service"
      via: "Socket Mode WebSocket"
      pattern: "bot responds to @mentions and fast-path commands"
---

<objective>
Smoke-test all v1.4-v1.6 features on the production VM to confirm they work end-to-end in the real environment.

Purpose: The bot was deployed in Phase 15. This phase verifies that digest changelog, fast-path commands, background batch crawl, and progress heartbeat all function correctly on the live VM -- not just locally.
Output: All 4 VRFY requirements confirmed working (or issues documented for fix).
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify digest changelog on VM (VRFY-01)</name>
  <files>n/a -- verification only, no files modified</files>
  <action>
    User verifies the daily digest changelog feature on the production VM.

    What was built: The daily digest includes a changelog section showing git commits and PRs the bot created, grouped by repository. If no git activity occurred, it shows "No changes" cleanly.

    How to verify:
    1. SSH to the VM: `gcloud compute ssh bot@superbot-vm --zone=us-west1-a`
    2. Check the digest scheduler is running: `journalctl -u superbot --since "today" | grep -i digest`
    3. To test immediately, trigger a digest manually if the bot supports it (e.g., ask the bot "send today's digest" or trigger via the scheduled task)
    4. In the Slack channel, confirm the digest message contains a "Changelog" section
    5. If the bot had git activity today, verify commits/PRs are listed and grouped by repo
    6. If the bot had no git activity today, verify the changelog shows "No changes" or is absent (not stale data)
  </action>
  <verify>User confirms digest changelog appears correctly in Slack</verify>
  <done>Digest changelog verified: posts with git activity grouped by repo, or shows "No changes" cleanly when idle</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify fast-path commands on VM (VRFY-02)</name>
  <files>n/a -- verification only, no files modified</files>
  <action>
    User verifies fast-path pattern matching for single-location EyeMed crawl and status queries on the production VM.

    What was built: Fast-path commands bypass the agent pipeline and edit the "Working on it." message in-place with results within seconds.

    How to verify:
    1. In the production Slack channel, send: `@SuperBot crawl eyemed DME 03.20` (adjust date as appropriate)
    2. Confirm the bot posts "Working on it." and then edits that same message in-place with a crawl confirmation (run ID, location, date) -- should complete within 5 seconds
    3. Send: `@SuperBot status on DME eyemed 03.16 to today` (adjust dates as appropriate)
    4. Confirm the bot posts "Working on it." and edits in-place with the status results -- should complete within 5 seconds
    5. Verify both commands did NOT start an agent session (no "started" progress message, no heartbeat)
    6. Send a normal question (e.g., `@SuperBot what is the latest commit on main?`) and confirm it falls through to the agent pipeline as usual
  </action>
  <verify>User confirms fast-path crawl and status commands edit messages in-place within seconds</verify>
  <done>Fast-path commands verified: crawl and status respond in-place within 5 seconds, unrecognized commands fall through to agent</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify background batch crawl on VM (VRFY-03)</name>
  <files>n/a -- verification only, no files modified</files>
  <action>
    User verifies batch crawl with background progress monitoring on the production VM.

    What was built: Batch crawl triggers all EyeMed Prefect deployments in parallel, posts progress updates every 2-3 minutes, and posts a final summary -- without blocking the agent queue.

    How to verify:
    1. In the production Slack channel, send: `@SuperBot crawl all sites for 03.20` (adjust date as appropriate)
    2. Confirm the bot responds within 10 seconds with how many locations were queued
    3. Wait 2-3 minutes and verify a progress update appears in the thread showing which locations are done, running, or errored
    4. While the crawl is running, send a separate task (e.g., `@SuperBot what time is it?`) and confirm it processes normally -- the batch crawl should NOT block the agent queue
    5. Wait for all crawl runs to finish and verify a final summary appears with per-location outcomes (files found, no disbursement, or error)
  </action>
  <verify>User confirms batch crawl posts progress updates and final summary without blocking agent</verify>
  <done>Background batch crawl verified: triggers all locations, progress updates every 2-3 min, final summary posted, agent queue not blocked</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify progress heartbeat on VM (VRFY-04)</name>
  <files>n/a -- verification only, no files modified</files>
  <action>
    User verifies progress heartbeat during long agent sessions on the production VM.

    What was built: During long agent sessions (>1 minute), the bot edits a single progress message with heartbeat updates showing last activity, turn count, and elapsed time. On completion, the message shows "Completed in Xm Ys".

    How to verify:
    1. In the production Slack channel, send a task that takes more than 1 minute (e.g., `@SuperBot search the mic_transformer codebase for all functions that handle VSP extraction and summarize what each one does`)
    2. Watch for the progress message -- after 60 seconds, it should be edited to show heartbeat format: "Still working... [Last Activity] | Turn X/25 | Ym Zs elapsed"
    3. Wait for a second heartbeat tick (every 3 minutes after the first) if the task runs long enough
    4. When the task completes, verify the final message shows elapsed time (e.g., "Completed in 2m 34s")
    5. Verify no further heartbeat edits occur after the final result is posted
    6. Optionally, check the VM logs: `gcloud compute ssh bot@superbot-vm --zone=us-west1-a -- journalctl -u superbot --since "5 minutes ago" | grep -i heartbeat`
  </action>
  <verify>User confirms heartbeat fires during session and stops cleanly on completion</verify>
  <done>Heartbeat verified: edits progress message with activity/turns/elapsed, stops on completion, final message shows elapsed time</done>
</task>

</tasks>

<verification>
All four VRFY requirements confirmed working on the production VM:
- VRFY-01: Digest changelog posts with git activity or clean "No changes"
- VRFY-02: Fast-path crawl and status commands respond in-place within seconds
- VRFY-03: Batch crawl runs in background with progress updates, does not block agent
- VRFY-04: Heartbeat fires during long sessions, stops cleanly on completion
</verification>

<success_criteria>
All 4 checkpoint tasks approved by the user, confirming v1.4-v1.6 features work on the production VM.
</success_criteria>

<output>
After completion, create `.planning/phases/16-live-verification/16-01-SUMMARY.md`
</output>
