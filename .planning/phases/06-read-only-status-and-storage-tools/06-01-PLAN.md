---
phase: 06-read-only-status-and-storage-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [RDTL-01, RDTL-02, RDTL-03]

must_haves:
  truths:
    - "VSP/EyeMed status query returns real processing data from GCS and database"
    - "S3 remit listing returns actual file names from the S3 bucket"
    - "GCS AIOUT listing returns actual file names from the GCS bucket"
    - "Pipeline status check returns multi-stage status with real data"
    - "Pipeline audit returns flow run history from the database"
  artifacts: []
  key_links:
    - from: "run_agent() on VM"
      to: "mic-transformer MCP vsp_status tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "vsp_status"
    - from: "run_agent() on VM"
      to: "mic-transformer MCP list_s3_remits tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "list_s3_remits"
    - from: "run_agent() on VM"
      to: "mic-transformer MCP list_gcs_aiout tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "list_gcs_aiout"
---

<objective>
Validate core read-only MCP tools (status checks, storage browsing, pipeline audit) by running them on the VM via direct agent calls. Confirms GCS, S3, and PostgreSQL credential pathways work under systemd.

Purpose: Prove that Nicole's most-used queries ("what's the VSP status?", "list remits in S3", "run a pipeline audit") return real data through SuperBot's MCP integration.
Output: Confirmed working tools documented in SUMMARY, any credential fixes applied on the VM.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vm-validation-and-mcp-wiring/05-02-SUMMARY.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate GCS and production API tools (vsp_status, eyemed_status, list_gcs_aiout, check_pipeline_status)</name>
  <files></files>
  <action>
SSH into the VM and run direct agent calls to test each tool. Use the pattern from Phase 5:

```bash
gcloud compute ssh superbot-vm --zone=us-west1-a --command="sudo -u bot bash -c 'cd /home/bot/super_bot && source .venv/bin/activate && python3 -c \"import asyncio; from bot.agent import run_agent; result = asyncio.run(run_agent(PROMPT, None, max_turns=5)); print(result)\"'"
```

Test each tool with these prompts (use Beverly as the test location, today's date or a known recent date with data):

1. **vsp_status**: "Check VSP processing status for Beverly for today"
   - Expected: Returns processing status data from GCS and database
   - Validates: GCS credentials, DB credentials

2. **eyemed_status**: "Check EyeMed processing status for Beverly for today"
   - Expected: Returns EyeMed processing status or "no data" (EyeMed may not have data for Beverly)
   - Validates: Same credential paths as VSP

3. **list_gcs_aiout**: "List AIOUT files in GCS for Beverly"
   - Expected: Returns file listing from GCS bucket
   - Validates: GCS credentials

4. **check_pipeline_status**: "Check full pipeline status for Beverly today"
   - Expected: Returns multi-stage pipeline status (crawler, extraction, reduction, posting)
   - Validates: GCS + S3 + GDrive credentials

If any tool fails due to credentials:
- Check if the relevant config/*.yml file exists and has correct content
- Fix by copying/updating the config file on the VM
- Re-run the failing tool to confirm the fix

Document each tool result: tool name, prompt used, success/failure, data returned (abbreviated), any fixes applied.
  </action>
  <verify>
Each of the 4 tools returns real data (not errors or empty results). At minimum:
- vsp_status returns structured status data
- list_gcs_aiout returns at least one file name
- check_pipeline_status returns stage-by-stage status
  </verify>
  <done>vsp_status, eyemed_status, list_gcs_aiout, and check_pipeline_status all return real data from the VM, confirming GCS and production API credential pathways work</done>
</task>

<task type="auto">
  <name>Task 2: Validate S3 and database tools (list_s3_remits, pipeline_audit, pipeline_stage_view)</name>
  <files></files>
  <action>
SSH into the VM and run direct agent calls for S3 and database-dependent tools:

1. **list_s3_remits**: "List VSP remittance PDFs in S3 for Beverly"
   - Expected: Returns file listing from AWS S3 bucket
   - Validates: AWS S3 credentials (may need ~/.aws/credentials or AWS env vars)
   - If fails: Check if AWS credentials exist on VM. May need to create ~/.aws/credentials for the bot user or add AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY to the systemd env file.

2. **pipeline_audit**: "Run a pipeline audit for recent VSP processing"
   - Expected: Returns flow run history from vsp_processing_audit_logs table
   - Validates: PostgreSQL (IrisMedAppDB) via config/db_irismedapp.yml

3. **pipeline_stage_view**: "Show pipeline stage view for Beverly today"
   - Expected: Returns stage-by-stage progress for one location/date
   - Validates: GCS + S3 + GDrive + DB (comprehensive)

If S3 credentials are missing:
- Check: `sudo -u bot bash -c 'cat ~/.aws/credentials 2>/dev/null || echo "NO AWS CREDS"'`
- If missing, check if there are AWS env vars in the systemd env file
- The mic-transformer tools may use boto3 which looks for credentials in standard locations

If DB connection fails:
- Check: `sudo -u bot bash -c 'cat /home/bot/mic_transformer/config/db_irismedapp.yml'`
- Verify the database host is reachable from the VM

Document each tool result with same format as Task 1.
  </action>
  <verify>
- list_s3_remits returns at least one file name from S3
- pipeline_audit returns audit log entries
- pipeline_stage_view returns stage data
  </verify>
  <done>list_s3_remits, pipeline_audit, and pipeline_stage_view return real data, confirming S3 and PostgreSQL credential pathways work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review core tool validation results</name>
  <action>Present the tool validation results from Tasks 1 and 2 for user review. Summarize which tools passed, which failed, and what credential fixes were applied.</action>
  <verify>User reviews and approves the core tool validation results</verify>
  <done>User confirms core credential pathways (GCS, S3, PostgreSQL) are working and approves proceeding to Plan 02</done>
  <what-built>Ran 7 read-only MCP tools on the VM covering GCS, S3, and PostgreSQL credential pathways. Each tool was tested with direct agent calls and results documented.</what-built>
  <how-to-verify>
    1. Review the tool results from Tasks 1 and 2
    2. Confirm that at least one tool per credential category returned real data:
       - GCS: vsp_status or list_gcs_aiout
       - S3: list_s3_remits
       - PostgreSQL: pipeline_audit
    3. Note any tools that failed and whether fixes were applied
    4. Confirm you're satisfied with the core tools before proceeding to Plan 02 (extended pathways)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 02, or describe issues to investigate</resume-signal>
</task>

</tasks>

<verification>
- At least 5 of 7 tools return real data from the VM
- GCS credential pathway confirmed working (at least 2 tools use it successfully)
- S3 credential pathway confirmed working (list_s3_remits returns files)
- PostgreSQL credential pathway confirmed working (pipeline_audit returns data)
</verification>

<success_criteria>
- vsp_status returns real VSP processing data
- list_s3_remits returns real S3 file listings
- pipeline_audit returns real database audit entries
- Any credential fixes are documented
</success_criteria>

<output>
After completion, create `.planning/phases/06-read-only-status-and-storage-tools/06-01-SUMMARY.md`
</output>
