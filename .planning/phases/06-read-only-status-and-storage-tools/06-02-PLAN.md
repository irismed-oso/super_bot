---
phase: 06-read-only-status-and-storage-tools
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [RDTL-04, RDTL-05, RDTL-06, RDTL-07, RDTL-08]

must_haves:
  truths:
    - "Azure mirror audit returns data freshness for CrystalPM locations"
    - "IVT ingestion audit returns health status from Prefect and prod-ivt DB"
    - "Prefect flow status check returns recent flow run data"
    - "Google Drive audit returns folder contents for remit files"
    - "Crawler location listing returns available locations"
  artifacts: []
  key_links:
    - from: "run_agent() on VM"
      to: "mic-transformer MCP azure_mirror_audit tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "azure_mirror_audit"
    - from: "run_agent() on VM"
      to: "mic-transformer MCP ivt_ingestion_audit tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "ivt_ingestion_audit"
    - from: "run_agent() on VM"
      to: "mic-transformer MCP gdrive_audit tool"
      via: "Claude Agent SDK stdio subprocess"
      pattern: "gdrive_audit"
---

<objective>
Validate extended read-only MCP tools covering Azure mirror, IVT ingestion, Prefect flow status, Google Drive audit, and crawler locations. Confirms SSH, Google Drive, and additional PostgreSQL credential pathways work under systemd.

Purpose: Complete the credential pathway validation by testing tools that use SSH tunnels, Google Drive API, and secondary PostgreSQL databases (crystalpm-mirror, prod-ivt). After this plan, all read-only tool categories are confirmed working.
Output: All read-only tools validated, credential gap documentation, SUMMARY with full results.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-read-only-status-and-storage-tools/06-01-SUMMARY.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Azure mirror, IVT ingestion, and Prefect tools (SSH + secondary DB pathways)</name>
  <files></files>
  <action>
SSH into the VM and run direct agent calls for tools that depend on SSH tunnels and secondary PostgreSQL databases.

Use the same direct agent pattern:
```bash
gcloud compute ssh superbot-vm --zone=us-west1-a --command="sudo -u bot bash -c 'cd /home/bot/super_bot && source .venv/bin/activate && python3 -c \"import asyncio; from bot.agent import run_agent; result = asyncio.run(run_agent(PROMPT, None, max_turns=5)); print(result)\"'"
```

1. **azure_mirror_audit**: "Check Azure mirror data freshness across all locations"
   - Expected: Returns freshness data for up to 24 CrystalPM mirror locations
   - Validates: PostgreSQL (crystalpm-mirror DB at 34.136.128.245) via psycopg2
   - Credential: Hardcoded `cpmm_dataloader@34.136.128.245` in tool code
   - If fails: Check if the VM can reach 34.136.128.245 on the PostgreSQL port (5432). May need VPC peering or Cloud SQL authorized networks.

2. **azure_mirror_run_status**: "Check recent Azure mirror sync run statuses"
   - Expected: Returns recent flow run statuses
   - Validates: SSH to production (ansible@136.111.85.127) + Prefect API
   - If fails: Check if SSH key exists for bot user: `sudo -u bot bash -c 'ls -la ~/.ssh/'`

3. **ivt_ingestion_audit**: "Audit IVT data ingestion health"
   - Expected: Returns Prefect flow health + prod-ivt DB status
   - Validates: SSH to production + PostgreSQL (prod-ivt at 34.136.128.245)
   - If fails: Same SSH key check as above, plus check DB connectivity

4. **check_prefect_flow_status**: "Check Prefect flow status for recent runs"
   - Expected: Returns recent Prefect flow run data via SSH + journalctl parsing
   - Validates: SSH to production server (ansible@136.111.85.127)

5. **get_prefect_logs**: "Get Prefect logs for the most recent flow run"
   - Expected: Returns log entries for a recent flow run
   - Validates: Prefect API (HTTP auth shen:tofu at 136.111.85.127:4200) + DB

If SSH fails:
- Check if bot user has SSH key: `sudo -u bot bash -c 'ls ~/.ssh/id_* 2>/dev/null || echo "NO SSH KEYS"'`
- If no SSH key, check if there's a shared key or if one needs to be generated and added to production's authorized_keys
- This may require user intervention (checkpoint in Task 3)

If Cloud SQL (34.136.128.245) is unreachable:
- Check firewall rules: `gcloud compute firewall-rules list --filter="name~superbot"`
- May need to add the VM's IP to Cloud SQL authorized networks
- This may require user intervention

Document each tool result: tool name, success/failure, data returned, any fixes needed.
  </action>
  <verify>
At least 3 of the 5 tools return real data. SSH connectivity to production server confirmed. Cloud SQL connectivity confirmed.
  </verify>
  <done>Azure mirror, IVT ingestion, and Prefect tools tested. SSH and secondary DB credential pathways confirmed or gaps documented.</done>
</task>

<task type="auto">
  <name>Task 2: Validate Google Drive audit and crawler location listing</name>
  <files></files>
  <action>
SSH into the VM and run direct agent calls for Google Drive and crawler tools:

1. **gdrive_audit**: "Audit Google Drive folders for VSP remit files for Beverly"
   - Expected: Returns GDrive folder contents showing remit file completeness
   - Validates: Google Drive API via service account (config/clinic_gdrive_config.yml)
   - If fails: Check if the GDrive service account config exists and has valid credentials
   - GDrive OAuth may need special handling -- the tool uses a service account, not user OAuth, so it should work without browser interaction

2. **list_crawler_locations**: "List all available crawler locations"
   - Expected: Returns list of configured location names
   - Validates: No external credentials needed (reads from internal config)
   - This should always succeed since it's just reading config data

3. **Additional read-only tools if time permits** (Claude's discretion):
   - `eyemed_crawler_audit`: "Audit EyeMed crawler results" -- validates S3 + DB
   - `provider_revenue`: "Show provider revenue data" -- validates DB (IrisMedAppDB)
   - `ocea_health_check`: "Run OCEA daily health check" -- validates DB
   - `for_eyes_autopost_analysis`: "Analyze For Eyes posting results" -- validates GCS + DB

Document results. For any failures, note whether the fix is straightforward (credential config) or requires user action (network rules, account setup).
  </action>
  <verify>
- list_crawler_locations returns location names
- gdrive_audit returns folder data or a clear credential error that can be documented
- At least one bonus tool tested if core tools pass quickly
  </verify>
  <done>Google Drive credential pathway tested, crawler locations confirmed, bonus tools tested where possible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review full read-only tool validation and confirm phase completion</name>
  <action>Present the complete tool validation results from both plans. Summarize all credential pathway statuses, working tools, failed tools with root causes, and any deferred issues for Phase 7.</action>
  <verify>User reviews and approves the full Phase 6 validation results</verify>
  <done>User confirms all credential pathways are validated (or gaps documented) and Phase 6 is complete</done>
  <what-built>Tested all read-only MCP tool categories on the VM. Validated credential pathways: GCS (Plan 01), S3 (Plan 01), PostgreSQL/IrisMedAppDB (Plan 01), PostgreSQL/crystalpm-mirror (this plan), PostgreSQL/prod-ivt (this plan), SSH to production (this plan), Google Drive (this plan), Prefect API (this plan).</what-built>
  <how-to-verify>
    1. Review the complete tool validation results from both plans
    2. Confirm credential pathway coverage:
       - GCS: confirmed in Plan 01
       - S3: confirmed in Plan 01
       - Google Drive: confirmed in this plan
       - PostgreSQL (primary): confirmed in Plan 01
       - PostgreSQL (secondary): confirmed in this plan (crystalpm-mirror, prod-ivt)
       - SSH to production: confirmed in this plan
       - Prefect API: confirmed in this plan
    3. Review any tools that failed and whether fixes were applied
    4. Confirm the phase success criteria are met:
       - User can query VSP/EyeMed status
       - User can browse S3/GCS storage
       - User can run pipeline audit
       - User can check Azure mirror freshness
       - User can audit IVT ingestion health
       - At least one tool per credential category returns valid data
    5. Note any deferred issues for Phase 7 (mutation tools)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 6, or describe issues needing resolution</resume-signal>
</task>

</tasks>

<verification>
- Azure mirror audit returns location freshness data
- Prefect flow status returns run data (or SSH connectivity confirmed)
- Google Drive audit returns folder data (or clear credential status documented)
- Crawler locations listing returns location names
- At least one tool from each remaining credential category (GDrive, SSH, secondary DB) works
</verification>

<success_criteria>
- All 4 credential categories validated: GCS (Plan 01), S3 (Plan 01), Google Drive (this plan), PostgreSQL (both plans)
- SSH to production server confirmed working for Prefect/Azure/IVT tools
- Any non-working tools documented with root cause and fix path
- Phase 6 success criteria from ROADMAP met
</success_criteria>

<output>
After completion, create `.planning/phases/06-read-only-status-and-storage-tools/06-02-SUMMARY.md`
</output>
