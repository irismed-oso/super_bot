---
phase: 17-deploy-foundation
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - DEPLOY.md
autonomous: false
requirements: [VRFY-01, VRFY-02, VRFY-03, VRFY-04]

must_haves:
  truths:
    - "Deploy commands work on the production VM via Slack"
    - "v1.4-v1.6 features verified working on the production VM"
  artifacts:
    - path: "DEPLOY.md"
      provides: "v1.8 deployment section with deploy commands and verification checklist"
      contains: "v1.8"
  key_links:
    - from: "bot/deploy.py"
      to: "prefect/deploy_superbot_flow.py"
      via: "Prefect API triggers the deploy flow"
      pattern: "deploy-superbot"
---

<objective>
Deploy Phase 17 code to the production VM and verify all deploy commands plus v1.4-v1.6 features work end-to-end.

Purpose: Validates that deploy-from-Slack, deploy status, deploy preview, and the active-task guard all work on the real VM. Also completes VRFY-01 through VRFY-04 (v1.4-v1.6 feature verification) which were deferred from Phase 16.

Output: Updated DEPLOY.md with v1.8 section, verified deploy commands on production.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-deploy-foundation/17-CONTEXT.md
@.planning/phases/17-deploy-foundation/17-01-SUMMARY.md
@.planning/phases/17-deploy-foundation/17-02-SUMMARY.md
@DEPLOY.md
@scripts/deploy_via_prefect.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DEPLOY.md with v1.8 section and deploy to VM</name>
  <files>DEPLOY.md</files>
  <action>
1. Add a "## v1.8: Production Ops" section to DEPLOY.md with:
   - New features: deploy from Slack, deploy status, deploy preview, active-task guard
   - Deploy commands reference:
     - `deploy super_bot` -- deploys super_bot with self-restart
     - `deploy mic_transformer` -- deploys mic_transformer with polling
     - `deploy status` -- shows current commit, branch, pending changes
     - `deploy preview super_bot` -- shows commits that would be deployed
     - `deploy force super_bot` -- deploys even if an agent task is running
   - Verification checklist for VRFY-01 through VRFY-04

2. Deploy to the VM using the existing deploy script:
   ```bash
   python scripts/deploy_via_prefect.py
   ```
   This pushes code, triggers Prefect deploy, and waits for completion.
  </action>
  <verify>
    <automated>cd /Users/hanjing/Documents/Code/GitLab/claude_workspace/super_bot && grep -c "v1.8" DEPLOY.md</automated>
  </verify>
  <done>DEPLOY.md has v1.8 section, code deployed to VM via Prefect</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deploy commands and v1.4-v1.6 features on production VM</name>
  <action>Human verification of deploy commands and VRFY features on the production VM.</action>
  <verify>All items in the checklist below pass</verify>
  <done>Deploy commands and VRFY-01 through VRFY-04 confirmed working on production VM</done>
  <what-built>
Deploy commands from Slack (deploy status, deploy preview, deploy super_bot, deploy mic_transformer) and v1.4-v1.6 feature verification.
  </what-built>
  <how-to-verify>
**Deploy commands (SDPL-01 through SDPL-05):**
1. In the Slack channel, type: `deploy status` -- verify you see commit SHA, branch, and pending changes for both repos
2. Type: `deploy preview super_bot` -- verify you see list of commits (or "nothing to deploy" if already current)
3. Type: `deploy super_bot` -- verify bot posts pre-restart message with SHA details, then restarts and posts "I'm back" to the thread
4. After restart, type: `deploy mic_transformer` -- verify bot triggers Prefect flow, shows polling progress, and reports success/failure

**v1.4-v1.6 verification (VRFY-01 through VRFY-04):**
5. VRFY-01 (Digest changelog): Check that the daily digest includes a changelog section (wait for next digest cycle or check recent digest messages)
6. VRFY-02 (Fast-path): Type `crawl eyemed DME [today's date]` and `status on DME eyemed [today]` -- verify fast-path responses
7. VRFY-03 (Background monitor): Type `crawl all sites for [today's date]` -- verify batch crawl triggers with background progress updates
8. VRFY-04 (Heartbeat): Start a long agent task and verify the progress message gets heartbeat edits every few minutes

Expected: All 5 deploy commands work as described. VRFY features confirmed working on VM.
  </how-to-verify>
  <resume-signal>Type "approved" with notes on which items passed, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Deploy status returns repo info for both super_bot and mic_transformer
- Deploy preview shows commit list or "nothing to deploy"
- Self-deploy (super_bot) posts pre-restart message and "I'm back" after restart
- mic_transformer deploy triggers Prefect flow and reports completion
- Active-task guard blocks deploy when agent is running (unless "force" used)
- VRFY-01 through VRFY-04 confirmed on production VM
</verification>

<success_criteria>
- All SDPL requirements verified working on the production VM
- All VRFY requirements verified working on the production VM
- DEPLOY.md updated with v1.8 section
</success_criteria>

<output>
After completion, create `.planning/phases/17-deploy-foundation/17-03-SUMMARY.md`
</output>
