---
phase: 08-response-timing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/handlers.py
  - bot/progress.py
autonomous: true
requirements:
  - TMG-01
  - TMG-02

must_haves:
  truths:
    - "When a task completes successfully, the Slack reply ends with an italic elapsed time footer like _Completed in 2m 34s_"
    - "When a task fails or times out, the Slack reply ends with an italic elapsed time footer like _Failed after 0m 45s_"
    - "The elapsed time format is always Xm Ys with minutes never omitted"
  artifacts:
    - path: "bot/progress.py"
      provides: "post_result accepts duration_s param and appends italic footer"
      contains: "duration_s"
    - path: "bot/handlers.py"
      provides: "Passes computed duration_s to progress.post_result"
      contains: "duration_s"
  key_links:
    - from: "bot/handlers.py"
      to: "bot/progress.py"
      via: "duration_s parameter passed to post_result()"
      pattern: "post_result.*duration_s"
---

<objective>
Add elapsed time display to all bot Slack replies (completion, error, and timeout messages).

Purpose: The team can see how long each task took without checking logs or timestamps.
Output: Modified bot/handlers.py and bot/progress.py with elapsed time footer on all result messages.
</objective>

<execution_context>
@/Users/hanjing/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hanjing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-response-timing/08-CONTEXT.md
@bot/handlers.py
@bot/progress.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add elapsed time footer to progress.py and wire from handlers.py</name>
  <files>bot/progress.py, bot/handlers.py</files>
  <action>
In bot/progress.py:

1. Add a helper function `_format_elapsed(duration_s: int) -> str` that converts seconds to the format `Xm Ys`. Always show both minutes and seconds (e.g. `0m 34s`, `2m 34s`, `10m 0s`). Use integer division: `minutes = duration_s // 60`, `seconds = duration_s % 60`.

2. Update the `post_result()` function signature to accept an optional `duration_s: int | None = None` parameter (after `is_code_task`).

3. Inside `post_result()`, after the PR/MR URL append block (after line ~120 `msg += f"\n\nPR: {pr_match.group(0)}"`), add the elapsed time footer. If `duration_s is not None`:
   - For error subtypes: append `\n\n_Failed after {_format_elapsed(duration_s)}_`
   - For success (non-error): append `\n\n_Completed in {_format_elapsed(duration_s)}_`
   - The footer MUST come after any PR URL line but before the markdown_to_mrkdwn conversion.

In bot/handlers.py:

4. In the `result_cb` function (around line 94), compute `duration_s = int(__import__("time").time() - task_started_at)` and pass it to `progress.post_result()`. The line currently reads:
   ```python
   await progress.post_result(client, channel, thread_ts, result, is_code_task_flag)
   ```
   Change to:
   ```python
   duration_s = int(__import__("time").time() - task_started_at)
   await progress.post_result(client, channel, thread_ts, result, is_code_task_flag, duration_s=duration_s)
   ```
   Note: `task_started_at` is already defined at line 84. Remove the duplicate `duration_s` computation from the activity_log block at line 102 and reuse the variable instead.
  </action>
  <verify>
    <automated>cd /Users/hanjing/Documents/Code/GitLab/claude_workspace/super_bot && python -c "
from bot.progress import _format_elapsed, post_result
import inspect

# Test format function
assert _format_elapsed(0) == '0m 0s', f'Got {_format_elapsed(0)}'
assert _format_elapsed(34) == '0m 34s', f'Got {_format_elapsed(34)}'
assert _format_elapsed(154) == '2m 34s', f'Got {_format_elapsed(154)}'
assert _format_elapsed(600) == '10m 0s', f'Got {_format_elapsed(600)}'

# Verify post_result accepts duration_s
sig = inspect.signature(post_result)
assert 'duration_s' in sig.parameters, 'post_result missing duration_s param'

print('All checks passed')
"</automated>
    <manual>Deploy to VM and send a test @mention. Verify the reply ends with an italic elapsed time line.</manual>
  </verify>
  <done>
    - _format_elapsed converts seconds to "Xm Ys" format correctly for all ranges
    - post_result() accepts duration_s parameter and appends italic footer to both success and error messages
    - handlers.py passes computed duration_s to post_result()
    - Footer appears after PR URL (if any) and before markdown conversion
  </done>
</task>

</tasks>

<verification>
1. `python -c "from bot.progress import _format_elapsed; print(_format_elapsed(154))"` outputs `2m 34s`
2. `python -c "from bot.progress import post_result; import inspect; sig = inspect.signature(post_result); assert 'duration_s' in sig.parameters"` passes
3. `grep -n "duration_s" bot/progress.py bot/handlers.py` shows the parameter in both files
4. `grep -n "_Completed in" bot/progress.py` confirms the success footer format
5. `grep -n "_Failed after" bot/progress.py` confirms the error footer format
</verification>

<success_criteria>
- bot/progress.py has _format_elapsed() function producing "Xm Ys" format
- bot/progress.py post_result() appends italic elapsed time footer to all message types
- bot/handlers.py passes duration_s to post_result()
- No existing functionality broken (post_result still works without duration_s via default None)
</success_criteria>

<output>
After completion, create `.planning/phases/08-response-timing/08-01-SUMMARY.md`
</output>
